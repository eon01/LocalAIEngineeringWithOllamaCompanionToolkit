# User-Friendly Interfaces for Ollama

## Local Chat UIs for Ollama

### Ollama App (Built-In)

### Open WebUI

### LibreChat

### AnythingLLM

### LobeChat

### Jan

### Hollama

### Comparison

## Installing and Using Open WebUI

### Installation

```bash
# replace with the current release tag
OWUI_VERSION=v0.9.6        

# Generate the secret key once and keep it. Reuse the same value on every
# update, or all logins get invalidated when the container is recreated.
WEBUI_SECRET_KEY=$(openssl rand -hex 32)
echo "$WEBUI_SECRET_KEY"   # save this somewhere safe

# Install Docker if you don't already have it.
curl -fsSL https://get.docker.com | sh

# Run the container
docker run -d \
  --name open-webui \
  --restart unless-stopped \
  --network=host \
  -e OLLAMA_BASE_URL=http://127.0.0.1:11434 \
  -e WEBUI_SECRET_KEY="${WEBUI_SECRET_KEY}" \
  -v open-webui:/app/backend/data \
  ghcr.io/open-webui/open-webui:${OWUI_VERSION}
```

```bash
ssh -Nf -p <ssh-port> <user>@<ip> -L <local-port>:localhost:8080
```

```bash
ssh -Nf -p 22 root@70.30.158.46 -L 3000:localhost:8080
```

### Running Open WebUI on a Separate Machine

```bash
sudo systemctl edit ollama
```

```ini
[Service]
Environment="OLLAMA_HOST=0.0.0.0:11434"
```

```bash
sudo systemctl daemon-reload
sudo systemctl restart ollama
ss -ltnp | grep 11434      # want 0.0.0.0:11434, not 127.0.0.1:11434
```

```bash
# Replace with the current release tag
OWUI_VERSION=v0.9.6        
# The port to run the UI on
UI_PORT=3000
# The IP or hostname of the Ollama machine
OLLAMA_HOST=[CHANGE_ME]
# Generate the secret key once and keep it. Reuse the same value on every
WEBUI_SECRET_KEY=$(openssl rand -hex 32)

docker run -d \
  --name open-webui \
  --restart unless-stopped \
  -p ${UI_PORT}:8080 \
  -e OLLAMA_BASE_URL=http://${OLLAMA_HOST}:11434 \
  -e WEBUI_SECRET_KEY="${WEBUI_SECRET_KEY}" \
  -v open-webui:/app/backend/data \
  ghcr.io/open-webui/open-webui:${OWUI_VERSION}
```

### The Features You Will Actually Use

### Common Failures

```bash
# 16384 for documents, 64000 for web search and agents
CTX=16384

# Set on the Ollama host, then restart the service
export OLLAMA_CONTEXT_LENGTH=$CTX   
```

```bash
ollama ps
# The CONTEXT column is the real allocation, not the model's trained maximum.
```
