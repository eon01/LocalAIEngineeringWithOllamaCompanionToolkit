# Building Advanced Agents: Streaming and Multi-Line Input

## Pass 3: Stream the Reply Token-by-Token and Accept Multi-Line Input

### Step 1: Read Multi-Line Input

```python
while True:
    line = input(prompt)
    if line == "":
        break
    lines.append(line)
    prompt = "      "
return "\n".join(lines)
```

### Step 2: Handle the Empty Submission in the Main Loop

```python
# If the user pressed Enter on an empty prompt, just loop back.
if user == "":
    continue
if user.strip() == "/bye":
    break
```

### Step 3: Raise the Client Timeout

```python
client = Client(host=OLLAMA_HOST, timeout=300)
```

### Step 4: Ask the API to Stream

```python
# stream=True makes the model send back its reply one tiny
# piece at a time instead of all at once at the end. We print
# each piece as it arrives (the "typing" effect) and also
# build up the full reply so we can save it to history.
print("Assistant > ", end="", flush=True)
full_reply = ""
for chunk in client.chat(
    model=OLLAMA_MODEL,
    messages=messages,
    stream=True,
):
    piece = chunk.message.content
    print(piece, end="", flush=True)
    full_reply += piece

# Final newline so the next 'You > ' starts on a fresh line.
print()
```

### Step 5: Save the Reassembled Reply to History

```python
messages.append(
    {"role": "assistant", "content": full_reply}
)
```

### Step 6: Run the REPL

```bash
# Change directory
cd $HOME/companion/code/repl

# run the REPL
uv run pass_3_repl_with_streaming.py
```

```text
> Write a short Python function
> that reverses a string and
> returns it. Add a docstring.
> 
Here's a short Python function that reverses a string:

def reverse_string(s: str) -> str:
    """Return s with its characters in reverse order."""
    return s[::-1]

> What does the docstring say?
The docstring says: "Return s with its characters in reverse order."

> /bye
```
