# Building Advanced Agents: Introduction

## Pass 1: A Bare-Minimum Chat Loop against a Local Ollama Model

### Step 1: Build the Client

```python
client = Client(host=OLLAMA_HOST)
```

### Step 2: The REPL Loop

```python
while True:
    user = input("You > ")

    if user.strip() == "/bye":
        break
```

### Step 3: Send One Message, Print One Reply

```python
response = client.chat(
    model=OLLAMA_MODEL,
    messages=[{"role": "user", "content": user}],
)
print(f"Assistant > {response.message.content}")
```

### Step 4: Running the REPL

```bash
# Change directory
cd $HOME/companion/code/repl

# Copy .env.example to .env
cp .env.example .env
```

```bash
ollama pull <model_name>
```

```bash
# Install dependencies
uv sync

# Run the REPL
uv run pass_1_repl_basic.py
```

```text
> My name is Bond, James Bond.
Greetings, Mr. Bond...

> What's my name?
I'm sorry, I don't have access to personal data about individuals...

> /bye
```
